# 工程化

链条太长。。。。链条的各段都可以自闭环验证（写demo）

> 例子：新开发 virtualwes给	3d用（**另一个团队**）  -----------> 同步开发2dcompos_demo来验证 virtualwes 实现的接口（即实现了virtualwes功能 + <font color='red'>接口验证的自闭环</font>）



与其他模块有调用关系，可以不关系其他模块，不验证其他模块：

> 自己模块自闭环成功，其他模块必然成功

# 如何裁剪代码

## 删减代码之传染性：

​	

```java
if (device->state_invalid) {  // 条件device->state_invalid 和 output_state->tear 都可以考虑删掉！！！！
	may_tear &= output_state->tear; // 1、结果may_tear这个变量没用，
}
```

传染性，可以干掉一大片代码

