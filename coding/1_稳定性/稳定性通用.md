# 目录

# 根

根本在于 **构造&复现问题场景**，其次才是定位问题

# 稳定性之monkey输入

```java
adb shell "monkey --ignore-crashes --ignore-timeouts --ignore-security-exceptions  --ignore-native-crashes --throttle 200 -v 500000"这个monkey发现的
```



# 稳定性之压测

高负载场景（增加应用个数、3D、播视频）

**构造**高负载（GPU降频）



# 猜测并code中主动构造问题场景

